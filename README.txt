rapd2 is a lightweight radare2 rap:// protocol server.

code is pretty ugly and probably has bugs, but it generally does the job
 
do not run on internet connected devices since it listens on 0.0.0.0.


